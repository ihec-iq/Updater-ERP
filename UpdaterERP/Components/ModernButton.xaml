<UserControl x:Class="UpdaterMsarERP.Components.ModernButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200" d:Content="BTN">
    <Button x:Name="BtnModern"
            Style="{StaticResource ModernButton}"
        Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Padding="{Binding Padding, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Click="BtnModern_Click"/>
</UserControl>